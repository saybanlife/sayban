/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: qARqpE4p5tZmJuNxFbTaPz
// Component: SZj80mEq7ul9

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import PaginationItem from "../../PaginationItem"; // plasmic-import: E0nCffMes_wS/component
import { _useGlobalVariants } from "./plasmic"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import sty from "./PlasmicPagination.module.css"; // plasmic-import: SZj80mEq7ul9/css

import Icon33Icon from "./icons/PlasmicIcon__Icon33"; // plasmic-import: MJBmXdq7GQ5D/icon
import Icon34Icon from "./icons/PlasmicIcon__Icon34"; // plasmic-import: L5-0jFcT5N2O/icon

createPlasmicElementProxy;

export type PlasmicPagination__VariantMembers = {};
export type PlasmicPagination__VariantsArgs = {};
type VariantPropType = keyof PlasmicPagination__VariantsArgs;
export const PlasmicPagination__VariantProps = new Array<VariantPropType>();

export type PlasmicPagination__ArgsType = {
  page?: number;
  onPageChange?: (val: string) => void;
  limit?: number;
  onLimitChange2?: (val: string) => void;
  totalPages?: number;
  onTotalPagesChange?: (val: string) => void;
};
type ArgPropType = keyof PlasmicPagination__ArgsType;
export const PlasmicPagination__ArgProps = new Array<ArgPropType>(
  "page",
  "onPageChange",
  "limit",
  "onLimitChange2",
  "totalPages",
  "onTotalPagesChange"
);

export type PlasmicPagination__OverridesType = {
  root?: Flex__<"div">;
  paginationItem?: Flex__<typeof PaginationItem>;
};

export interface DefaultPaginationProps {
  page?: number;
  onPageChange?: (val: string) => void;
  limit?: number;
  onLimitChange2?: (val: string) => void;
  totalPages?: number;
  onTotalPagesChange?: (val: string) => void;
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicPagination__RenderFunc(props: {
  variants: PlasmicPagination__VariantsArgs;
  args: PlasmicPagination__ArgsType;
  overrides: PlasmicPagination__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const stateSpecs: Parameters<typeof useDollarState>[0] = React.useMemo(
    () => [
      {
        path: "page",
        type: "writable",
        variableType: "number",

        valueProp: "page",
        onChangeProp: "onPageChange"
      },
      {
        path: "limit",
        type: "writable",
        variableType: "number",

        valueProp: "limit",
        onChangeProp: "onLimitChange2"
      },
      {
        path: "totalPages",
        type: "writable",
        variableType: "number",

        valueProp: "totalPages",
        onChangeProp: "onTotalPagesChange"
      }
    ],
    [$props, $ctx, $refs]
  );

  const $state = useDollarState(stateSpecs, {
    $props,
    $ctx,
    $queries: {},
    $q: {},
    $refs
  });

  const styleTokensClassNames = _useStyleTokens();

  return (
    <div
      data-plasmic-name={"root"}
      data-plasmic-override={overrides.root}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "all",
        "root_reset_qARqpE4p5tZmJuNxFbTaPz",
        "plasmic_default_styles",
        "plasmic_mixins",
        styleTokensClassNames,
        sty.root
      )}
    >
      <div
        aria-disabled={(() => {
          try {
            return ($state.page || 1) <= 1;
          } catch (e) {
            if (
              e instanceof TypeError ||
              e?.plasmicType === "PlasmicUndefinedDataError"
            ) {
              return undefined;
            }
            throw e;
          }
        })()}
        className={classNames("all", sty.freeBox__zm5O)}
        onClick={async event => {
          const $steps = {};

          $steps["runCode"] = true
            ? (() => {
                const actionArgs = {
                  customFunction: async () => {
                    return ($state.page = Math.max(($state.page || 1) - 1, 1));
                  }
                };
                return (({ customFunction }) => {
                  return customFunction();
                })?.apply(null, [actionArgs]);
              })()
            : undefined;
          if (
            $steps["runCode"] != null &&
            typeof $steps["runCode"] === "object" &&
            typeof $steps["runCode"].then === "function"
          ) {
            $steps["runCode"] = await $steps["runCode"];
          }
        }}
      >
        <Icon33Icon className={classNames("all", sty.svg__iH5F)} role={"img"} />
      </div>
      {(_par => (!_par ? [] : Array.isArray(_par) ? _par : [_par]))(
        (() => {
          try {
            return (() => {
              const currentPage = $state.page || 1;
              const totalPages = $state.totalPages || 1;
              if (totalPages <= 1) return [1];
              const pages = [1];
              for (
                let page = currentPage + 1;
                page <= Math.min(currentPage + 4, totalPages - 1);
                page++
              ) {
                pages.push(page);
              }
              if (totalPages > 6 && currentPage + 4 < totalPages) {
                pages.push("...");
              }
              pages.push(totalPages);
              return pages;
            })();
          } catch (e) {
            if (
              e instanceof TypeError ||
              e?.plasmicType === "PlasmicUndefinedDataError"
            ) {
              return [];
            }
            throw e;
          }
        })()
      ).map((__plasmic_item_0, __plasmic_idx_0) => {
        const currentItem = __plasmic_item_0;
        const currentIndex = __plasmic_idx_0;
        return (
          <PaginationItem
            data-plasmic-name={"paginationItem"}
            data-plasmic-override={overrides.paginationItem}
            ariaDisabled={(() => {
              try {
                return currentItem == "...";
              } catch (e) {
                if (
                  e instanceof TypeError ||
                  e?.plasmicType === "PlasmicUndefinedDataError"
                ) {
                  return undefined;
                }
                throw e;
              }
            })()}
            className={classNames("__wab_instance", sty.paginationItem)}
            key={currentIndex}
            onClick={async event => {
              const $steps = {};

              $steps["updatePage"] = true
                ? (() => {
                    const actionArgs = {
                      variable: {
                        objRoot: $state,
                        variablePath: ["page"]
                      },
                      operation: 0,
                      value: currentItem
                    };
                    return (({ variable, value, startIndex, deleteCount }) => {
                      if (!variable) {
                        return;
                      }
                      const { objRoot, variablePath } = variable;

                      $stateSet(objRoot, variablePath, value);
                      return value;
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["updatePage"] != null &&
                typeof $steps["updatePage"] === "object" &&
                typeof $steps["updatePage"].then === "function"
              ) {
                $steps["updatePage"] = await $steps["updatePage"];
              }
            }}
            selected={(() => {
              try {
                return currentItem === $state.page;
              } catch (e) {
                if (
                  e instanceof TypeError ||
                  e?.plasmicType === "PlasmicUndefinedDataError"
                ) {
                  return [];
                }
                throw e;
              }
            })()}
            text={(() => {
              try {
                return currentItem;
              } catch (e) {
                if (
                  e instanceof TypeError ||
                  e?.plasmicType === "PlasmicUndefinedDataError"
                ) {
                  return undefined;
                }
                throw e;
              }
            })()}
          />
        );
      })}
      <div
        aria-disabled={(() => {
          try {
            return ($state.page || 1) >= ($state.totalPages || 1);
          } catch (e) {
            if (
              e instanceof TypeError ||
              e?.plasmicType === "PlasmicUndefinedDataError"
            ) {
              return undefined;
            }
            throw e;
          }
        })()}
        className={classNames("all", sty.freeBox___5XAcs)}
        onClick={async event => {
          const $steps = {};

          $steps["runCode"] = true
            ? (() => {
                const actionArgs = {
                  customFunction: async () => {
                    return ($state.page = Math.min(
                      ($state.page || 1) + 1,
                      $state.totalPages || 1
                    ));
                  }
                };
                return (({ customFunction }) => {
                  return customFunction();
                })?.apply(null, [actionArgs]);
              })()
            : undefined;
          if (
            $steps["runCode"] != null &&
            typeof $steps["runCode"] === "object" &&
            typeof $steps["runCode"].then === "function"
          ) {
            $steps["runCode"] = await $steps["runCode"];
          }
        }}
      >
        <Icon34Icon
          className={classNames("all", sty.svg__lxwzF)}
          role={"img"}
        />
      </div>
    </div>
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  root: ["root", "paginationItem"],
  paginationItem: ["paginationItem"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  root: "div";
  paginationItem: typeof PaginationItem;
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicPagination__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicPagination__VariantsArgs;
    args?: PlasmicPagination__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicPagination__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicPagination__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicPagination__ArgProps,
          internalVariantPropNames: PlasmicPagination__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicPagination__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "root") {
    func.displayName = "PlasmicPagination";
  } else {
    func.displayName = `PlasmicPagination.${nodeName}`;
  }
  return func;
}

export const PlasmicPagination = Object.assign(
  // Top-level PlasmicPagination renders the root element
  makeNodeComponent("root"),
  {
    // Helper components rendering sub-elements
    paginationItem: makeNodeComponent("paginationItem"),

    // Metadata about props expected for PlasmicPagination
    internalVariantProps: PlasmicPagination__VariantProps,
    internalArgProps: PlasmicPagination__ArgProps
  }
);

export default PlasmicPagination;
/* prettier-ignore-end */
