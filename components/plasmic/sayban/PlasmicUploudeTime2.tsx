/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: qARqpE4p5tZmJuNxFbTaPz
// Component: x5TCDbKvWReV

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import { _useGlobalVariants } from "./plasmic"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import sty from "./PlasmicUploudeTime2.module.css"; // plasmic-import: x5TCDbKvWReV/css

createPlasmicElementProxy;

export type PlasmicUploudeTime2__VariantMembers = {};
export type PlasmicUploudeTime2__VariantsArgs = {};
type VariantPropType = keyof PlasmicUploudeTime2__VariantsArgs;
export const PlasmicUploudeTime2__VariantProps = new Array<VariantPropType>();

export type PlasmicUploudeTime2__ArgsType = { posttime?: any };
type ArgPropType = keyof PlasmicUploudeTime2__ArgsType;
export const PlasmicUploudeTime2__ArgProps = new Array<ArgPropType>("posttime");

export type PlasmicUploudeTime2__OverridesType = {
  root?: Flex__<"div">;
  text?: Flex__<"div">;
};

export interface DefaultUploudeTime2Props {
  posttime?: any;
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicUploudeTime2__RenderFunc(props: {
  variants: PlasmicUploudeTime2__VariantsArgs;
  args: PlasmicUploudeTime2__ArgsType;
  overrides: PlasmicUploudeTime2__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const stateSpecs: Parameters<typeof useDollarState>[0] = React.useMemo(
    () => [
      {
        path: "time",
        type: "private",
        variableType: "object",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) =>
          (() => {
            try {
              return $props.posttime;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return {};
              }
              throw e;
            }
          })()
      }
    ],
    [$props, $ctx, $refs]
  );

  const $state = useDollarState(stateSpecs, {
    $props,
    $ctx,
    $queries: {},
    $q: {},
    $refs
  });

  const styleTokensClassNames = _useStyleTokens();

  return (
    <div
      data-plasmic-name={"root"}
      data-plasmic-override={overrides.root}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "all",
        "root_reset_qARqpE4p5tZmJuNxFbTaPz",
        "plasmic_default_styles",
        "plasmic_mixins",
        styleTokensClassNames,
        sty.root
      )}
    >
      <div
        data-plasmic-name={"text"}
        data-plasmic-override={overrides.text}
        className={classNames("all", "__wab_text", sty.text)}
      >
        <React.Fragment>
          {(() => {
            try {
              return (() => {
                function pad(n) {
                  return n.toString().padStart(2, "0");
                }
                const today = new Date();
                const updatedAt = $state.time;
                const postTime =
                  `${updatedAt.year}-${pad(updatedAt.month)}-${pad(updatedAt.day)}T` +
                  `${pad(updatedAt.hour)}:${pad(updatedAt.minute)}:${pad(updatedAt.second)}+03:30`;
                const inputDate = new Date(postTime);
                const diffInMillis = today.getTime() - inputDate.getTime();
                const diffInMinutes = Math.floor(diffInMillis / (1000 * 60));
                const diffInHours = Math.floor(diffInMillis / (1000 * 3600));
                const diffInDays = Math.floor(
                  diffInMillis / (1000 * 3600 * 24)
                );
                const diffInWeeks = Math.floor(diffInDays / 7);
                const diffInMonths = Math.floor(diffInDays / 30);
                let result = "";
                if (diffInMinutes < 60) {
                  result = `${diffInMinutes} دقیقه قبل`;
                } else if (diffInHours < 24) {
                  result = `${diffInHours} ساعت قبل`;
                } else if (diffInDays < 7) {
                  result = `${diffInDays} روز قبل`;
                } else if (diffInDays < 30) {
                  const diffInWeeks = Math.floor(diffInDays / 7);
                  result = `${diffInWeeks} هفته قبل`;
                } else if (diffInMonths < 12) {
                  result = `${diffInMonths} ماه قبل`;
                } else {
                  const years = Math.floor(diffInMonths / 12);
                  const remainingMonths = diffInMonths % 12;
                  if (remainingMonths === 0) {
                    result = `${years} سال قبل`;
                  } else {
                    result = `${years} سال و ${remainingMonths} ماه قبل`;
                  }
                }
                return result;
              })();
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return "\u0632\u0645\u0627\u0646 \u0628\u0627\u0631\u06af\u0632\u0627\u0631\u06cc ";
              }
              throw e;
            }
          })()}
        </React.Fragment>
      </div>
    </div>
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  root: ["root", "text"],
  text: ["text"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  root: "div";
  text: "div";
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicUploudeTime2__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicUploudeTime2__VariantsArgs;
    args?: PlasmicUploudeTime2__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicUploudeTime2__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicUploudeTime2__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicUploudeTime2__ArgProps,
          internalVariantPropNames: PlasmicUploudeTime2__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicUploudeTime2__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "root") {
    func.displayName = "PlasmicUploudeTime2";
  } else {
    func.displayName = `PlasmicUploudeTime2.${nodeName}`;
  }
  return func;
}

export const PlasmicUploudeTime2 = Object.assign(
  // Top-level PlasmicUploudeTime2 renders the root element
  makeNodeComponent("root"),
  {
    // Helper components rendering sub-elements
    text: makeNodeComponent("text"),

    // Metadata about props expected for PlasmicUploudeTime2
    internalVariantProps: PlasmicUploudeTime2__VariantProps,
    internalArgProps: PlasmicUploudeTime2__ArgProps
  }
);

export default PlasmicUploudeTime2;
/* prettier-ignore-end */
