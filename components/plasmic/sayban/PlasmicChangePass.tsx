/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: qARqpE4p5tZmJuNxFbTaPz
// Component: Ga93_OGgGtUe

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import TextInput from "../../TextInput"; // plasmic-import: lMgENIWzjnK0/component
import Hide from "../../Hide"; // plasmic-import: 6tyxrSjs9tps/component
import { AntdProgress } from "@plasmicpkgs/antd5/skinny/registerProgress";
import Check from "../../Check"; // plasmic-import: jHhGioxaI9lI/component
import Button from "../../Button"; // plasmic-import: 2MRRFY7jUAge/component
import { _useGlobalVariants } from "./plasmic"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import sty from "./PlasmicChangePass.module.css"; // plasmic-import: Ga93_OGgGtUe/css

import CircleIcon from "./icons/PlasmicIcon__Circle"; // plasmic-import: 4RgfxZWAffAT/icon
import ChevronDownIcon from "./icons/PlasmicIcon__ChevronDown"; // plasmic-import: cDVOBX0F9d9g/icon

createPlasmicElementProxy;

export type PlasmicChangePass__VariantMembers = {};
export type PlasmicChangePass__VariantsArgs = {};
type VariantPropType = keyof PlasmicChangePass__VariantsArgs;
export const PlasmicChangePass__VariantProps = new Array<VariantPropType>();

export type PlasmicChangePass__ArgsType = {
  passwordNow?: string;
  onPasswordNowChange?: (val: string) => void;
  onSavePassChange?: (val: string) => void;
  passwordRepeat?: string;
  onPasswordRepeatChange?: (val: string) => void;
  userinfo?: any;
  onUserinfoChange?: (val: string) => void;
  passwordNew?: string;
  onPasswordNewChange?: (val: string) => void;
};
type ArgPropType = keyof PlasmicChangePass__ArgsType;
export const PlasmicChangePass__ArgProps = new Array<ArgPropType>(
  "passwordNow",
  "onPasswordNowChange",
  "onSavePassChange",
  "passwordRepeat",
  "onPasswordRepeatChange",
  "userinfo",
  "onUserinfoChange",
  "passwordNew",
  "onPasswordNewChange"
);

export type PlasmicChangePass__OverridesType = {
  root?: Flex__<"div">;
  password?: Flex__<typeof TextInput>;
  hide?: Flex__<typeof Hide>;
  password2?: Flex__<typeof TextInput>;
  hide3?: Flex__<typeof Hide>;
  progress?: Flex__<typeof AntdProgress>;
  repeatPassword?: Flex__<typeof TextInput>;
  hide2?: Flex__<typeof Hide>;
  check?: Flex__<typeof Check>;
  button2?: Flex__<typeof Button>;
};

export interface DefaultChangePassProps {
  passwordNow?: string;
  onPasswordNowChange?: (val: string) => void;
  onSavePassChange?: (val: string) => void;
  passwordRepeat?: string;
  onPasswordRepeatChange?: (val: string) => void;
  userinfo?: any;
  onUserinfoChange?: (val: string) => void;
  passwordNew?: string;
  onPasswordNewChange?: (val: string) => void;
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicChangePass__RenderFunc(props: {
  variants: PlasmicChangePass__VariantsArgs;
  args: PlasmicChangePass__ArgsType;
  overrides: PlasmicChangePass__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const stateSpecs: Parameters<typeof useDollarState>[0] = React.useMemo(
    () => [
      {
        path: "password.value",
        type: "writable",
        variableType: "text",

        valueProp: "passwordNow",
        onChangeProp: "onPasswordNowChange"
      },
      {
        path: "hide.show",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => undefined
      },
      {
        path: "hide.type",
        type: "private",
        variableType: "text",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => "password"
      },
      {
        path: "check.isSelected",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => false
      },
      {
        path: "button2.loading",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => undefined
      },
      {
        path: "savePass",
        type: "readonly",
        variableType: "number",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => 0,

        onChangeProp: "onSavePassChange"
      },
      {
        path: "repeatPassword.value",
        type: "writable",
        variableType: "text",

        valueProp: "passwordRepeat",
        onChangeProp: "onPasswordRepeatChange"
      },
      {
        path: "hide2.show",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => undefined
      },
      {
        path: "hide2.type",
        type: "private",
        variableType: "text",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => "password"
      },
      {
        path: "userinfo",
        type: "writable",
        variableType: "object",

        valueProp: "userinfo",
        onChangeProp: "onUserinfoChange"
      },
      {
        path: "password2.value",
        type: "writable",
        variableType: "text",

        valueProp: "passwordNew",
        onChangeProp: "onPasswordNewChange"
      },
      {
        path: "hide3.show",
        type: "private",
        variableType: "boolean",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => undefined
      },
      {
        path: "hide3.type",
        type: "private",
        variableType: "text",
        initFunc: ({ $props, $state, $queries, $q, $ctx }) => "password"
      }
    ],
    [$props, $ctx, $refs]
  );

  const $globalActions = useGlobalActions?.();

  const $state = useDollarState(stateSpecs, {
    $props,
    $ctx,
    $queries: {},
    $q: {},
    $refs
  });

  const styleTokensClassNames = _useStyleTokens();

  return (
    <div
      data-plasmic-name={"root"}
      data-plasmic-override={overrides.root}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "all",
        "root_reset_qARqpE4p5tZmJuNxFbTaPz",
        "plasmic_default_styles",
        "plasmic_mixins",
        styleTokensClassNames,
        sty.root,
        "dark"
      )}
    >
      <div className={classNames("all", sty.freeBox___3SIo5)}>
        <TextInput
          data-plasmic-name={"password"}
          data-plasmic-override={overrides.password}
          className={classNames("__wab_instance", sty.password)}
          inputType={(() => {
            try {
              return $state.hide.type;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return "password";
              }
              throw e;
            }
          })()}
          onChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["password", "value"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          placeholder={
            "\u0631\u0645\u0632 \u0639\u0628\u0648\u0631 \u0641\u0639\u0644\u06cc"
          }
          size={"langh"}
          type={"line"}
          value={generateStateValueProp($state, ["password", "value"])}
        />

        <Hide
          data-plasmic-name={"hide"}
          data-plasmic-override={overrides.hide}
          className={classNames("__wab_instance", sty.hide)}
          onClick={async event => {
            const $steps = {};

            $steps["runCode"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide.show = !$state.hide.show;
                        return ($state.hide.type = "text");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode"] != null &&
              typeof $steps["runCode"] === "object" &&
              typeof $steps["runCode"].then === "function"
            ) {
              $steps["runCode"] = await $steps["runCode"];
            }

            $steps["invokeGlobalAction"] = true
              ? (() => {
                  const actionArgs = { args: [3000] };
                  return $globalActions["Fragment.wait"]?.apply(null, [
                    ...actionArgs.args
                  ]);
                })()
              : undefined;
            if (
              $steps["invokeGlobalAction"] != null &&
              typeof $steps["invokeGlobalAction"] === "object" &&
              typeof $steps["invokeGlobalAction"].then === "function"
            ) {
              $steps["invokeGlobalAction"] = await $steps["invokeGlobalAction"];
            }

            $steps["runCode2"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide.show = !$state.hide.show;
                        return ($state.hide.type = "password");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode2"] != null &&
              typeof $steps["runCode2"] === "object" &&
              typeof $steps["runCode2"].then === "function"
            ) {
              $steps["runCode2"] = await $steps["runCode2"];
            }
          }}
          onShowChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide", "show"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          onTypeChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide", "type"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          show={generateStateValueProp($state, ["hide", "show"])}
          type={generateStateValueProp($state, ["hide", "type"])}
        />
      </div>
      <div className={classNames("all", sty.freeBox__jPMc)}>
        <TextInput
          data-plasmic-name={"password2"}
          data-plasmic-override={overrides.password2}
          className={classNames("__wab_instance", sty.password2)}
          inputType={(() => {
            try {
              return $state.hide3.type;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return "password";
              }
              throw e;
            }
          })()}
          onChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["password2", "value"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }

            (async val => {
              const $steps = {};

              $steps["runCode"] = true
                ? (() => {
                    const actionArgs = {
                      customFunction: async () => {
                        return (() => {
                          let score = 0;
                          const password = $state.password2?.value || "";
                          const checks = {
                            length: password.length >= 8,
                            lowercase: /[a-z]/.test(password),
                            uppercase: /[A-Z]/.test(password),
                            number: /\d/.test(password),
                            special: /[@$!%*?&.]/.test(password)
                          };
                          Object.values(checks).forEach(c => {
                            if (c) score += 20;
                          });
                          return ($state.savePass = score);
                        })();
                      }
                    };
                    return (({ customFunction }) => {
                      return customFunction();
                    })?.apply(null, [actionArgs]);
                  })()
                : undefined;
              if (
                $steps["runCode"] != null &&
                typeof $steps["runCode"] === "object" &&
                typeof $steps["runCode"].then === "function"
              ) {
                $steps["runCode"] = await $steps["runCode"];
              }
            }).apply(null, eventArgs);
          }}
          placeholder={
            "\u0631\u0645\u0632 \u0639\u0628\u0648\u0631 \u062c\u062f\u06cc\u062f"
          }
          size={"langh"}
          type={"line"}
          value={generateStateValueProp($state, ["password2", "value"])}
        />

        <Hide
          data-plasmic-name={"hide3"}
          data-plasmic-override={overrides.hide3}
          className={classNames("__wab_instance", sty.hide3)}
          onClick={async event => {
            const $steps = {};

            $steps["runCode"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide3.show = !$state.hide3.show;
                        return ($state.hide3.type = "text");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode"] != null &&
              typeof $steps["runCode"] === "object" &&
              typeof $steps["runCode"].then === "function"
            ) {
              $steps["runCode"] = await $steps["runCode"];
            }

            $steps["invokeGlobalAction"] = true
              ? (() => {
                  const actionArgs = { args: [3000] };
                  return $globalActions["Fragment.wait"]?.apply(null, [
                    ...actionArgs.args
                  ]);
                })()
              : undefined;
            if (
              $steps["invokeGlobalAction"] != null &&
              typeof $steps["invokeGlobalAction"] === "object" &&
              typeof $steps["invokeGlobalAction"].then === "function"
            ) {
              $steps["invokeGlobalAction"] = await $steps["invokeGlobalAction"];
            }

            $steps["runCode2"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide3.show = !$state.hide3.show;
                        return ($state.hide3.type = "password");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode2"] != null &&
              typeof $steps["runCode2"] === "object" &&
              typeof $steps["runCode2"].then === "function"
            ) {
              $steps["runCode2"] = await $steps["runCode2"];
            }
          }}
          onShowChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide3", "show"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          onTypeChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide3", "type"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          show={generateStateValueProp($state, ["hide3", "show"])}
          type={generateStateValueProp($state, ["hide3", "type"])}
        />
      </div>
      <AntdProgress
        data-plasmic-name={"progress"}
        data-plasmic-override={overrides.progress}
        className={classNames("__wab_instance", sty.progress)}
        percent={(() => {
          try {
            return $state.savePass;
          } catch (e) {
            if (
              e instanceof TypeError ||
              e?.plasmicType === "PlasmicUndefinedDataError"
            ) {
              return 60;
            }
            throw e;
          }
        })()}
        showInfo={false}
        status={"exception"}
        stepColors={[]}
        steps={5}
        strokeColor={(() => {
          try {
            return (() => {
              if ($state.savePass <= 40) {
                return "#e03131";
              } else if ($state.savePass <= 80) {
                return "#f08c00";
              } else {
                return "#2f9e44";
              }
            })();
          } catch (e) {
            if (
              e instanceof TypeError ||
              e?.plasmicType === "PlasmicUndefinedDataError"
            ) {
              return "var(--token-vrTybXLeM0dG)";
            }
            throw e;
          }
        })()}
        strokeLinecap={"round"}
        type={"line"}
      />

      <div className={classNames("all", "__wab_text", sty.text__ijQzo)}>
        {
          "\u0631\u0645\u0632 \u0639\u0628\u0648\u0631 \u0628\u0627\u06cc\u062f \u062d\u062f\u0627\u0642\u0644 \u06f8 \u06a9\u0627\u0631\u0627\u06a9\u062a\u0631 \u0648 \u062a\u0631\u06a9\u06cc\u0628\u06cc \u0627\u0632 \u062d\u0631\u0648\u0641 \u0648 \u0627\u0639\u062f\u0627\u062f \u0628\u0627\u0634\u062f."
        }
      </div>
      <div className={classNames("all", sty.freeBox__dxBDk)}>
        <TextInput
          data-plasmic-name={"repeatPassword"}
          data-plasmic-override={overrides.repeatPassword}
          className={classNames("__wab_instance", sty.repeatPassword)}
          error={(() => {
            try {
              return (
                $state.repeatPassword.value.length > 0 &&
                $state.repeatPassword.value != $state.password2.value
              );
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return [];
              }
              throw e;
            }
          })()}
          inputType={(() => {
            try {
              return $state.hide2.type;
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return "password";
              }
              throw e;
            }
          })()}
          onChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, [
              "repeatPassword",
              "value"
            ]).apply(null, eventArgs);

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          placeholder={
            "\u062a\u06a9\u0631\u0627\u0631 \u0631\u0645\u0632 \u0639\u0628\u0648\u0631 \u062c\u062f\u06cc\u062f"
          }
          size={"langh"}
          type={"line"}
          value={generateStateValueProp($state, ["repeatPassword", "value"])}
        />

        <Hide
          data-plasmic-name={"hide2"}
          data-plasmic-override={overrides.hide2}
          className={classNames("__wab_instance", sty.hide2)}
          onClick={async event => {
            const $steps = {};

            $steps["runCode"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide2.show = !$state.hide2.show;
                        return ($state.hide2.type = "text");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode"] != null &&
              typeof $steps["runCode"] === "object" &&
              typeof $steps["runCode"].then === "function"
            ) {
              $steps["runCode"] = await $steps["runCode"];
            }

            $steps["invokeGlobalAction"] = true
              ? (() => {
                  const actionArgs = { args: [3000] };
                  return $globalActions["Fragment.wait"]?.apply(null, [
                    ...actionArgs.args
                  ]);
                })()
              : undefined;
            if (
              $steps["invokeGlobalAction"] != null &&
              typeof $steps["invokeGlobalAction"] === "object" &&
              typeof $steps["invokeGlobalAction"].then === "function"
            ) {
              $steps["invokeGlobalAction"] = await $steps["invokeGlobalAction"];
            }

            $steps["runCode2"] = true
              ? (() => {
                  const actionArgs = {
                    customFunction: async () => {
                      return (() => {
                        $state.hide2.show = !$state.hide2.show;
                        return ($state.hide2.type = "password");
                      })();
                    }
                  };
                  return (({ customFunction }) => {
                    return customFunction();
                  })?.apply(null, [actionArgs]);
                })()
              : undefined;
            if (
              $steps["runCode2"] != null &&
              typeof $steps["runCode2"] === "object" &&
              typeof $steps["runCode2"].then === "function"
            ) {
              $steps["runCode2"] = await $steps["runCode2"];
            }
          }}
          onShowChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide2", "show"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          onTypeChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["hide2", "type"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
          show={generateStateValueProp($state, ["hide2", "show"])}
          type={generateStateValueProp($state, ["hide2", "type"])}
        />
      </div>
      <div className={classNames("all", sty.freeBox__lo9HO)}>
        <Check
          data-plasmic-name={"check"}
          data-plasmic-override={overrides.check}
          className={classNames("__wab_instance", sty.check)}
          isSelected={generateStateValueProp($state, ["check", "isSelected"])}
          label={
            <div className={classNames("all", "__wab_text", sty.text___3LhRt)}>
              {
                "\u0645\u0631\u0627 \u0628\u0647 \u062e\u0627\u0637\u0631 \u0628\u0633\u067e\u0627\u0631"
              }
            </div>
          }
          onChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["check", "isSelected"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
        />

        <Button
          data-plasmic-name={"button2"}
          data-plasmic-override={overrides.button2}
          className={classNames("__wab_instance", sty.button2)}
          color={"clear"}
          label={
            <div className={classNames("all", "__wab_text", sty.text___1V8LF)}>
              {
                "\u0631\u0645\u0632 \u0631\u0627 \u0641\u0631\u0627\u0645\u0648\u0634 \u06a9\u0631\u062f\u0647\u200c\u0627\u06cc\u062f\u061f"
              }
            </div>
          }
          loading={generateStateValueProp($state, ["button2", "loading"])}
          onLoadingChange={async (...eventArgs: any) => {
            generateStateOnChangeProp($state, ["button2", "loading"]).apply(
              null,
              eventArgs
            );

            if (
              eventArgs.length > 1 &&
              eventArgs[1] &&
              eventArgs[1]._plasmic_state_init_
            ) {
              return;
            }
          }}
        />
      </div>
    </div>
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  root: [
    "root",
    "password",
    "hide",
    "password2",
    "hide3",
    "progress",
    "repeatPassword",
    "hide2",
    "check",
    "button2"
  ],
  password: ["password"],
  hide: ["hide"],
  password2: ["password2"],
  hide3: ["hide3"],
  progress: ["progress"],
  repeatPassword: ["repeatPassword"],
  hide2: ["hide2"],
  check: ["check"],
  button2: ["button2"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  root: "div";
  password: typeof TextInput;
  hide: typeof Hide;
  password2: typeof TextInput;
  hide3: typeof Hide;
  progress: typeof AntdProgress;
  repeatPassword: typeof TextInput;
  hide2: typeof Hide;
  check: typeof Check;
  button2: typeof Button;
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicChangePass__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicChangePass__VariantsArgs;
    args?: PlasmicChangePass__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicChangePass__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicChangePass__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicChangePass__ArgProps,
          internalVariantPropNames: PlasmicChangePass__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicChangePass__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "root") {
    func.displayName = "PlasmicChangePass";
  } else {
    func.displayName = `PlasmicChangePass.${nodeName}`;
  }
  return func;
}

export const PlasmicChangePass = Object.assign(
  // Top-level PlasmicChangePass renders the root element
  makeNodeComponent("root"),
  {
    // Helper components rendering sub-elements
    password: makeNodeComponent("password"),
    hide: makeNodeComponent("hide"),
    password2: makeNodeComponent("password2"),
    hide3: makeNodeComponent("hide3"),
    progress: makeNodeComponent("progress"),
    repeatPassword: makeNodeComponent("repeatPassword"),
    hide2: makeNodeComponent("hide2"),
    check: makeNodeComponent("check"),
    button2: makeNodeComponent("button2"),

    // Metadata about props expected for PlasmicChangePass
    internalVariantProps: PlasmicChangePass__VariantProps,
    internalArgProps: PlasmicChangePass__ArgProps
  }
);

export default PlasmicChangePass;
/* prettier-ignore-end */
