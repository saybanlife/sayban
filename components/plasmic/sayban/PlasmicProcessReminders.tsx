/* eslint-disable */
/* tslint:disable */
// @ts-nocheck
/* prettier-ignore-start */

/** @jsxRuntime classic */
/** @jsx createPlasmicElementProxy */
/** @jsxFrag React.Fragment */

// This class is auto-generated by Plasmic; please do not edit!
// Plasmic Project: qARqpE4p5tZmJuNxFbTaPz
// Component: hth_nosTucNX

import * as React from "react";

import Head from "next/head";
import Link, { LinkProps } from "next/link";
import { useRouter } from "next/router";

import {
  Flex as Flex__,
  MultiChoiceArg,
  PlasmicDataSourceContextProvider as PlasmicDataSourceContextProvider__,
  PlasmicIcon as PlasmicIcon__,
  PlasmicImg as PlasmicImg__,
  PlasmicLink as PlasmicLink__,
  PlasmicPageGuard as PlasmicPageGuard__,
  SingleBooleanChoiceArg,
  SingleChoiceArg,
  Stack as Stack__,
  StrictProps,
  Trans as Trans__,
  classNames,
  createPlasmicElementProxy,
  deriveRenderOpts,
  ensureGlobalVariants,
  generateOnMutateForSpec,
  generateStateOnChangeProp,
  generateStateOnChangePropForCodeComponents,
  generateStateValueProp,
  get as $stateGet,
  hasVariant,
  initializeCodeComponentStates,
  initializePlasmicStates,
  makeFragment,
  omit,
  pick,
  renderPlasmicSlot,
  set as $stateSet,
  useCurrentUser,
  useDollarState,
  usePlasmicTranslator,
  useTrigger,
  wrapWithClassName
} from "@plasmicapp/react-web";
import {
  DataCtxReader as DataCtxReader__,
  useDataEnv,
  useGlobalActions
} from "@plasmicapp/react-web/lib/host";

import { _useGlobalVariants } from "./plasmic"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/projectModule
import { _useStyleTokens } from "./PlasmicStyleTokensProvider"; // plasmic-import: qARqpE4p5tZmJuNxFbTaPz/styleTokensProvider

import "@plasmicapp/react-web/lib/plasmic.css";

import sty from "./PlasmicProcessReminders.module.css"; // plasmic-import: hth_nosTucNX/css

createPlasmicElementProxy;

export type PlasmicProcessReminders__VariantMembers = {};
export type PlasmicProcessReminders__VariantsArgs = {};
type VariantPropType = keyof PlasmicProcessReminders__VariantsArgs;
export const PlasmicProcessReminders__VariantProps =
  new Array<VariantPropType>();

export type PlasmicProcessReminders__ArgsType = { data?: any };
type ArgPropType = keyof PlasmicProcessReminders__ArgsType;
export const PlasmicProcessReminders__ArgProps = new Array<ArgPropType>("data");

export type PlasmicProcessReminders__OverridesType = {
  root?: Flex__<"div">;
};

export interface DefaultProcessRemindersProps {
  data?: any;
  className?: string;
}

const $$ = {};

function useNextRouter() {
  try {
    return useRouter();
  } catch {}
  return undefined;
}

function PlasmicProcessReminders__RenderFunc(props: {
  variants: PlasmicProcessReminders__VariantsArgs;
  args: PlasmicProcessReminders__ArgsType;
  overrides: PlasmicProcessReminders__OverridesType;
  forNode?: string;
}) {
  const { variants, overrides, forNode } = props;

  const args = React.useMemo(
    () =>
      Object.assign(
        {},
        Object.fromEntries(
          Object.entries(props.args).filter(([_, v]) => v !== undefined)
        )
      ),
    [props.args]
  );

  const $props = {
    ...args,
    ...variants
  };

  const __nextRouter = useNextRouter();

  const $ctx = useDataEnv?.() || {};
  const refsRef = React.useRef({});
  const $refs = refsRef.current;

  const styleTokensClassNames = _useStyleTokens();

  return (
    <div
      data-plasmic-name={"root"}
      data-plasmic-override={overrides.root}
      data-plasmic-root={true}
      data-plasmic-for-node={forNode}
      className={classNames(
        "all",
        "__wab_text",
        "root_reset_qARqpE4p5tZmJuNxFbTaPz",
        "plasmic_default_styles",
        "plasmic_mixins",
        styleTokensClassNames,
        sty.root
      )}
    >
      <div
        className={"__wab_expr_html_text"}
        dangerouslySetInnerHTML={{
          __html: (() => {
            try {
              return (() => {
                function processReminders(reminders, channels) {
                  const startDate = new Date();
                  const oneMonthLater = new Date();
                  oneMonthLater.setMonth(oneMonthLater.getMonth() + 1);
                  function getDatesBetween(start, finish) {
                    const dates = [];
                    const d = new Date(start);
                    while (d <= finish) {
                      dates.push(new Date(d));
                      d.setDate(d.getDate() + 1);
                    }
                    return dates;
                  }
                  const results = reminders.map(rem => {
                    let times = [];
                    try {
                      times = Array.isArray(rem.times)
                        ? rem.times
                        : JSON.parse(rem.times || "[]");
                    } catch {
                      times = [];
                    }
                    const timesPerDay = times.length || 0;
                    const pre = Array.isArray(rem.pre) ? rem.pre : [];
                    const preCount = pre.length || 0;
                    let chList = [];
                    try {
                      chList = Array.isArray(rem.chanels)
                        ? rem.chanels
                        : JSON.parse(rem.chanels || "[]");
                    } catch {
                      chList = [];
                    }
                    const finishTime = rem.finishTime
                      ? new Date(rem.finishTime)
                      : oneMonthLater;
                    const endDate =
                      finishTime < oneMonthLater ? finishTime : oneMonthLater;
                    let activeDates = [];
                    if (rem.dates) {
                      try {
                        const dateList = Array.isArray(rem.dates)
                          ? rem.dates
                          : JSON.parse(rem.dates || "[]");
                        activeDates = dateList.map(d => new Date(d));
                      } catch {
                        activeDates = [];
                      }
                    }
                    if (!activeDates.length) {
                      activeDates = getDatesBetween(startDate, endDate);
                    }
                    const validDays = activeDates.length;
                    const totalNotifications =
                      validDays * timesPerDay + preCount;
                    return {
                      id: rem.id,
                      name: rem.name,
                      timesPerDay,
                      activeDays: validDays,
                      preCount,
                      totalNotifications,
                      channels: chList
                    };
                  });
                  return {
                    results,
                    startDate,
                    endDate: oneMonthLater
                  };
                }
                if ($props?.data) {
                  const a = processReminders([$props.data]);
                  const item = a?.results?.[0];
                  const period =
                    $props?.data?.schedule_type === "everyDay" ? "ماه" : "سال";
                  return item
                    ? `<span style="font-weight:bold; color:#8254C6;">
         ${item.totalNotifications}
       </span>
       <span> عدد در ${period}</span>`
                    : `<span></span>`;
                }
                if ($props?.data) {
                  const a = processReminders([$props.data]);
                  const item = a?.results?.[0];
                  const total = item ? item.activeDays * item.timesPerDay : 0;
                  const period =
                    $props?.data?.schedule_type === "everyDay" ? "ماه" : "سال";
                  return `
    <span style="font-weight:bold; color:#8254C6;">
      ${total}
    </span>
    <span> عدد در ${period}</span>
  `;
                } else {
                  return `<span></span>`;
                }
              })();
            } catch (e) {
              if (
                e instanceof TypeError ||
                e?.plasmicType === "PlasmicUndefinedDataError"
              ) {
                return "";
              }
              throw e;
            }
          })()
        }}
      />
    </div>
  ) as React.ReactElement | null;
}

const PlasmicDescendants = {
  root: ["root"]
} as const;
type NodeNameType = keyof typeof PlasmicDescendants;
type DescendantsType<T extends NodeNameType> =
  (typeof PlasmicDescendants)[T][number];
type NodeDefaultElementType = {
  root: "div";
};

type ReservedPropsType = "variants" | "args" | "overrides";
type NodeOverridesType<T extends NodeNameType> = Pick<
  PlasmicProcessReminders__OverridesType,
  DescendantsType<T>
>;
type NodeComponentProps<T extends NodeNameType> =
  // Explicitly specify variants, args, and overrides as objects
  {
    variants?: PlasmicProcessReminders__VariantsArgs;
    args?: PlasmicProcessReminders__ArgsType;
    overrides?: NodeOverridesType<T>;
  } &
    // Specify variants directly as props
    Omit<PlasmicProcessReminders__VariantsArgs, ReservedPropsType> &
    // Specify args directly as props
    Omit<PlasmicProcessReminders__ArgsType, ReservedPropsType> &
    // Specify overrides for each element directly as props
    Omit<
      NodeOverridesType<T>,
      ReservedPropsType | VariantPropType | ArgPropType
    > &
    // Specify props for the root element
    Omit<
      Partial<React.ComponentProps<NodeDefaultElementType[T]>>,
      ReservedPropsType | VariantPropType | ArgPropType | DescendantsType<T>
    >;

function makeNodeComponent<NodeName extends NodeNameType>(nodeName: NodeName) {
  type PropsType = NodeComponentProps<NodeName> & { key?: React.Key };
  const func = function <T extends PropsType>(
    props: T & StrictProps<T, PropsType>
  ) {
    const { variants, args, overrides } = React.useMemo(
      () =>
        deriveRenderOpts(props, {
          name: nodeName,
          descendantNames: PlasmicDescendants[nodeName],
          internalArgPropNames: PlasmicProcessReminders__ArgProps,
          internalVariantPropNames: PlasmicProcessReminders__VariantProps
        }),
      [props, nodeName]
    );
    return PlasmicProcessReminders__RenderFunc({
      variants,
      args,
      overrides,
      forNode: nodeName
    });
  };
  if (nodeName === "root") {
    func.displayName = "PlasmicProcessReminders";
  } else {
    func.displayName = `PlasmicProcessReminders.${nodeName}`;
  }
  return func;
}

export const PlasmicProcessReminders = Object.assign(
  // Top-level PlasmicProcessReminders renders the root element
  makeNodeComponent("root"),
  {
    // Helper components rendering sub-elements

    // Metadata about props expected for PlasmicProcessReminders
    internalVariantProps: PlasmicProcessReminders__VariantProps,
    internalArgProps: PlasmicProcessReminders__ArgProps
  }
);

export default PlasmicProcessReminders;
/* prettier-ignore-end */
